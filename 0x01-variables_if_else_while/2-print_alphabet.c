#include <stdio.h>
/**
 * main - A program that print alphabet lower case function
 * return 0 (success)
 */
int main(void)
{
	int n = 97;

	while (n <= 122)
	{
		putchar(n);
		n++;
	}
	putchar('\n');

return (0);
}
