#include "main.h"
#include <unistd.h>
/**
 * _putchar.c - writes the character c stdout
 * @c: the character print
 *
 * Return: on success 1
 */
int _putchar.c(char c)
{
	return (write(1, &c, 1));
}
